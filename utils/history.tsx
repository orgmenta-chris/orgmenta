// Placeholder
// Undo/redo functionality.

// UNDO

export const doHistoryUndo = () => {
  return "todo - undo (reverse an action)";
};

// REDO

export const doHistoryRedo = () => {
  return "todo - redo (reverse an undo)";
};

// ITEMS

export const requestHistoryItems = () => {
  return "todo - show the history items that you can fo forward/backwards in";
};


import { ActivityIndicator, ActivityIndicatorProps } from 'react-native';

// SPINNER

export const ViewIndicatorSpinner = ActivityIndicator

export type TypeIndicatorSpinner = ActivityIndicatorProps

// TEXT

export const ViewIndicatorText = 'todo'

export type TypeIndicatorText = 'todo'

// BADGE

// A component to show coloured dot/badge (with optional content. If left empty, will just display a dot.)
// see here for example: https://reactnativeelements.com/docs/1.2.0/badge (do not use that library though, it's non-performant and that version isn't maintained.)
export const ViewIndicatorBadge = 'todo'

export type TypeIndicatorDot = 'todo'
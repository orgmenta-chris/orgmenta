
import { Image, ImageProps, ImageBackground, ImageBackgroundProps, ImageBackgroundBase, ImageBackgroundComponent } from "react-native";

export const ViewImageMain = Image;
export type TypeImageMain = ImageProps;

export const ViewImageBackground = ImageBackground;
export type TypeImageBackground = ImageBackgroundProps;
